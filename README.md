# Projeto de Ordenação — RGM 045620920
Algoritmos: Insertion, Quick (Lomuto), Merge.
Veja o código-fonte nos arquivos incluídos. Compile com:
```
gcc -std=c11 main.c sorts.c -o ordena
```
Execute:
```
./ordena
```
Saída: CSV com passos e tempo em ms.
